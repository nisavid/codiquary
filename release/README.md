# Release metadata

This directory will own versioned source and artifact manifests, contract
version inventory, dependency inventory, conformance and qualification
references, migration guidance, and required review evidence for immutable
public releases.

The repository currently has no release-manifest instance, tag, package, or
release. A no-bypass repository ruleset blocks every tag operation until the
separately governed release authority exists.
